package org.BankManagement.GUI;

/*
* It is the first page which will appear when the application is
* launched. This is the login page which will provide people a
* choice formed by 2 variants -> login as an employee or login as
* a user. It is one of the main page, thus it will extend the
* BaseFrame abstract class
* */

public class LoginFrame extends BaseFrame {
    public LoginFrame() {
        super("Login page");
        setVisible(true);
    }

    @Override
    protected void addGuiElements() {

    }
}
